# 디자인 기록 — 세법 쟁점 대장

## 제품 경계

이 사이트는 기존 학습 앱의 진도판이 아니라, 법령을 자유롭게 읽는 별도 공개 아틀라스다. 첫 화면은 다섯 법률의 대장과 50개 행을 보여 주고, 행을 열면 한 쟁점을 원문에서 판례까지 한 방향으로 읽게 한다.

## 방향 계약

- **THESIS**: 조문을 먼저 열고 결론을 바꾸는 사실·증빙·판례를 연결한다.
- **OWN-WORLD**: 기존 사건배당부의 따뜻한 석재, 종이 표면, 잉크 네이비, 공식 파랑, 얇은 규선을 이어 간다.
- **STORY**: 법률 선택 → 쟁점 행 → 답을 보기 전 질문 → 원문 → 초보자 해설 → 판단 지도 → 합성 사례·증빙 → 판례 → 필요한 회계.
- **FIRST VIEWPORT**: 첫 화면에서 법률별 10개 위치와 검색·필터를 확인하며, 큰 숫자만 보여 주는 영웅 영역을 만들지 않는다.
- **FORM**: 카드 벽 대신 번호가 붙은 직각 대장 행과 세로 규선을 사용한다.
- **FINISH**: `unreviewed and undocumented is unfinished`; 이 빌드는 검증 명령, 판정, 이 문서 기록으로 끝난다.

## 시각 토큰

| 역할 | 값 | 사용처 |
|---|---|---|
| 잉크 | `#16283d` | 제목·핵심 텍스트·두꺼운 기준선 |
| 잉크 보조 | `#33465a` | 본문 설명·판단 문장 |
| 석재 | `#f4f1e8` | 페이지 배경 |
| 종이 | `#fffdf7` | 헤더·원문·학습 블록 |
| 공식 파랑 | `#155d9c` | 링크·이동 |
| 장부 청록 | `#0b6f69` | 단계·조문·모듈 라벨 |
| 경고 적갈 | `#9b3d36` | 조사 가설·검토 라벨 |
| 규선 | `#b7b4ab` / `#6d7780` | 행·표·섹션 경계 |

그림자, 그라데이션, 장식 이모지, 햄버거 메뉴는 사용하지 않는다. 굵은 선과 배경 대비만으로 현재 위치를 표시한다.

## 타이포그래피와 밀도

기본 본문은 17px, 줄 간격 1.72, `word-break: keep-all`과 `overflow-wrap: anywhere`를 사용한다. 제목은 짧은 세리프 계열, 설명은 시스템 산세리프 계열로 구분하며, 리더 본문은 약 72자 폭으로 제한한다. 모바일에서는 제목 크기를 줄이되 본문 글자 크기는 유지한다.

## 화면 구조

### 인덱스

상단 소개는 한 문장과 얇은 규선으로 끝낸다. 다섯 법률 띠는 2~5열로 줄바꿈되고, 아래에는 검색·법률 필터와 결과 수가 있다. 쟁점은 번호·법률/난도·제목·핵심 질문·모듈·읽기 링크가 있는 대장 행이다. JavaScript가 꺼져도 모든 행이 보인다.

### 쟁점 리더

첫 화면에는 브레드크럼, 쟁점 제목, “답을 보기 전” 질문과 내부 이동 링크를 둔다. 본문은 법령 원문(열린 `details`)을 가장 먼저 보여 주고, 초보자용 한 문장과 원고의 판단 지도·사례·증빙·판례를 차례로 쌓는다. 판례가 없으면 추정 사건을 만들지 않고 공식 색인 부재를 명시한다. 회계·세무조정은 실제 금액·장부·신고 결과를 바꾸는 원고에만 표시하며, 시험형 섹션은 독립 문제 데이터가 없으므로 넣지 않는다.

## 모바일·접근성 계약

- 390px 폭에서 가로 넘침이 없어야 한다.
- 링크·입력·`summary`는 최소 44px 터치 영역을 갖는다.
- `main` 랜드마크와 본문 건너뛰기 링크를 모든 페이지에 넣는다.
- 키보드 `focus-visible`, `aria-live` 결과 수, 레이블이 있는 필터를 제공한다.
- 긴 표는 모바일에서 행을 세로로 쌓고, 셀의 `data-label`로 열 이름을 다시 표시한다.
- `prefers-reduced-motion`과 인쇄 스타일에서도 읽는 순서를 유지한다.

## 콘텐츠·출처 원칙

2026-09-03에 기존 `tax-study`의 공개 원고와 법령·판례 색인을 스냅샷했다. 법률별 10개 목록은 `content/issue-selection.json`에서 선언하며, 생성 스크립트가 개수·중복·공식 URL을 실패 조건으로 검증한다. 합성 사례는 화면에 명시하고, 판례 카드는 국가법령정보센터 또는 대법원 공식 PDF로 연결되는 자료만 사용한다.
